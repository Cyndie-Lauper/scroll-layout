import React, { JSX } from "react"
import { TextBlock } from "../core"
import "@/styles/base.css"

export const ProcessSection = (): JSX.Element => (
  <TextBlock
    title="Process"
    description="The creative process begins with stillness and observation, letting the moment speak before any action is taken. It’s about immersing in the environment, feeling the quiet shifts in light, texture, and mood. Rather than forcing a scene, there’s a deep patience—waiting for the right interplay of shadow or the soft touch of light on a surface."
  />
)
